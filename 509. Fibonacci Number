var fib = function(n) {
    var output=[0,1];
    if(n<2){
        return output[n];
    }
    for(let i=2;i<=n;i++){     
        output.push(output[output.length-1]+output[output.length-2]);
    }
    return output[output.length-1];
};
