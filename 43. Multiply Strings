class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        ans1=int(num1)
        ans2=int(num2)
        return str(ans1*ans2)
        
        or
        
class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        return str(int(num1)*int(num2))
